{
  "name": "keyconnections-api",
  "version": "1.0.0",
  "description": "Google Cloud Run function for KeyConnections API",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "deploy": "gcloud run deploy keyconnections-api --source . --platform managed --region us-central1 --allow-unauthenticated"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "dotenv": "^16.3.1",
    "nodemailer": "^6.9.8"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}
