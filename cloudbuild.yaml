# Cloud Build configuration with proper logging options
options:
  logging: CLOUD_LOGGING_ONLY

steps:
  # Build and push the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/keyconnections-api:$COMMIT_SHA', '-f', 'api/Dockerfile', 'api']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/keyconnections-api:$COMMIT_SHA']

  # Deploy to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'keyconnections-api'
      - '--image'
      - 'gcr.io/$PROJECT_ID/keyconnections-api:$COMMIT_SHA'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'

images:
  - 'gcr.io/$PROJECT_ID/keyconnections-api:$COMMIT_SHA'
